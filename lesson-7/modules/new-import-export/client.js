const marvel = (hero) => {
    console.log(`Hello, ${hero}!`);
  }
  
const dc = (hero) => {
  console.log(`Bye, ${hero}!`);
}
const testPhrase = 'Hey, I exist';

export default testPhrase;
  
export {marvel, dc};